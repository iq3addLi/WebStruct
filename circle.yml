dependencies:
    override:
        - eval "$(curl -sL https://apt.vapor.sh)"
        - sudo apt-get install clang-3.6 swift
        - sudo chmod -R a+rx /usr/

test:
    override:
        - swift build
        - swift build -c release
        - swift test -s WebStructTests.OpenWebAPITests
