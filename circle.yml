machine:
    timezone:
        Asia/Tokyo

dependencies:
    override:
        - sudo apt-get install swift
        - sudo chmod -R a+rx /usr/

test:
    override:
        - /usr/bin/swift build
        - /usr/bin/swift build -c release
        - /usr/bin/swift test -s WebStructTests.OpenWebAPITests
